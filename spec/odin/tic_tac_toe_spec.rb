# frozen_string_literal: true

RSpec.describe Odin::TicTacToe do
  it "has a version number" do
    expect(Odin::TicTacToe::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
